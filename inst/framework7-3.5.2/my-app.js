var app = new Framework7({
  // App root element
  root: '#app',
  // App Name
  name: 'My App',
  // App id
  id: 'com.myapp.test',
  theme: 'auto'
  // ... other parameters
});

var theme = app.theme('ios');
//var mainView = app.views.create('.view-main');
